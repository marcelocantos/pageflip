# Rust
/target/
**/*.rs.bk
Cargo.lock.bak

# IDE / OS
.vscode/
.idea/
.DS_Store

# Claude Code agent worktrees
.claude/

# pageflip (Go orchestrator) build artefact
/meetcat/pageflip

# pageflip-capture (Rust engine) per-run output directories
pageflip-capture-*/

# pageflip (Go orchestrator) per-meeting artefact directories
pageflip-*/

# Legacy meetcat-* artefact directories (from before the rename — keep
# ignored so old meetings don't get accidentally committed).
meetcat-*/
